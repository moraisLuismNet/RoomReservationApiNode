export interface ReservationStatusDTO {
  statusId: number;
  name: string;
}

export interface CreateReservationStatusDTO {
  name: string;
}

export interface UpdateReservationStatusDTO {
  name: string;
}
